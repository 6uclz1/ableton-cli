---
name: ableton-cli
description: Control and inspect Ableton Live via uv run ableton-cli. Use when an agent needs deterministic setup, transport, track/clip operations, browser loading, or stable JSON command mappings in this repository.
---

# ableton-cli (project skill)

The authoritative skill content lives at `skills/ableton-cli/SKILL.md` in this repository (single source of truth).

When this skill is selected, read `skills/ableton-cli/SKILL.md` immediately and follow it for all `uv run ableton-cli ...` workflows. Do not duplicate command catalogs anywhere else.
